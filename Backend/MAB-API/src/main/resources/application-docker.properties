# ?? Docker profile ? overrides datasource URL only ???????????????????????????
# When running inside docker-compose, PostgreSQL is reachable via the
# service name "postgres" (defined in docker-compose.yml), not localhost.
# All other settings are inherited from application.properties.

spring.datasource.url=jdbc:postgresql://postgres:5432/myaccounting_dev
spring.datasource.username=mab_app
spring.datasource.password=A2sQcLf.p4t_KdBa
